# Copy this file to backend/.env and adjust as needed.
# Every value here has a safe default in app/config.py, so the app runs
# WITHOUT a .env during early development. We add real secrets (LLM keys,
# Supabase URL) to this file at the step that introduces each component.

APP_NAME=Concord
ENVIRONMENT=development
